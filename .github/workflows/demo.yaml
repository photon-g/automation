
name: Sample
on: [push]

jobs:
  build:
    name: launch sample
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: echo string
      run: echo "oblivion"
    
     
